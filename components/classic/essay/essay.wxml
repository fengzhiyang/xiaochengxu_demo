<!-- components/classic/essay/essay.wxml -->
<view class="classic-container" hidden="{{hidden}}">
    <image class="classic-img" src="{{img}}" />
    <image class="tag" src="/images/easy@tag.png" />
    <text class="content">{{content}}</text>
</view>